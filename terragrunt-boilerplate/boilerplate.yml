variables:
  - name: Type
    type: enum
    options:
      - single-account
      - multi-account
    validate:
      - required

hooks:
  after:
    - command: bash
      args:
        - -c
        - '{{ if eq .Type "single-account" }}boilerplate --template-url "github.com/codefactoryhu/terragrunt-aws-boilerplate//terragrunt-boilerplate?ref=classic-single-account" --output-folder {{ outputFolder }}{{ else if eq .Type "multi-account" }}boilerplate --template-url "github.com/codefactoryhu/terragrunt-aws-boilerplate//terragrunt-boilerplate?ref=classic-multi-account" --output-folder {{ outputFolder }}{{ end }}'
